<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.mapper.ReplyMapper">

        <!-- 댓글 등록 -->
        <insert id="insert">
                <selectKey
                        order="AFTER"
                        keyProperty="rno"
                        resultType="long">
                        SELECT LAST_INSERT_ID()
                </selectKey>
                INSERT INTO
                tbl_reply
                (
                bno,
                replytext,
                replyer
                )
                VALUES
                (
                #{bno},
                #{replyText},
                #{replyer}
                )
        </insert>
        <!-- // 댓글 등록 -->

        <!-- 댓글 조회 -->
        <select
                id="read"
                resultType="ReplyDTO">
                SELECT
                *
                FROM
                tbl_reply
                WHERE
                1 = 1
                AND rno = ${rno}
        </select>
        <!-- // 댓글 조회 -->

        <!-- 댓글 삭제 -->
        <update id="delete">
                UPDATE
                tbl_reply
                SET
                delflag = true
                WHERE
                1 = 1
                AND rno = #{rno}
        </update>
        <!-- // 댓글 삭제 -->

        <!-- 댓글 수정 -->
        <update id="update">
                UPDATE
                tbl_reply
                SET
                replytext = #{replyText},
                updatedate = NOW()
                WHERE
                1 = 1
                AND rno = #{rno}
        </update>
        <!-- // 댓글 수정 -->

        <!-- 댓글 목록 조회 -->
        <select
                id="listOfBoard"
                resultType="ReplyDTO">
                SELECT
                *
                FROM
                tbl_reply
                WHERE
                1 = 1
                AND bno = #{bno}
                AND rno > 0
                ORDER BY rno ASC
                LIMIT #{limit}
                OFFSET #{skip}
        </select>
        <!-- // 댓글 목록 조회 -->

        <!-- 특정 게시물의 댓글수 획득 -->
        <select
                id="getNumberOfRepliesByBno"
                resultType="int">
                SELECT count(rno)
                FROM tbl_reply
                WHERE 1 = 1
                AND rno > 0
                AND delflag = false
                AND bno = #{bno}
        </select>
        <!-- //특정 게시물의 댓글수 획득 -->

</mapper>
